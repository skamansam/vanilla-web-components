/*
  Storybook's Vitest addon applies project preview annotations (parameters,
  loaders, decorators) automatically since Storybook 10.3, so no custom setup
  is required here. This file remains as a placeholder for any future Vitest
  setup code (e.g. mocking globals or disabling animations in snapshots).
*/
